<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user_story.name %>
</p>

<%= link_to 'Edit', edit_user_story_path(@user_story) if policy(@user_story).edit? %>
<%= link_to 'Back', user_stories_path %>
